// The charCodeAt() method returns the Unicode of the character
//  at a specified index (position) in a string:

let text = "Hello World!";
let char = text.charCodeAt(0);
console.log(char); // 72

// Project Use Case: Create custom sort or encryption.